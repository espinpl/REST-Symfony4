{% extends 'base.html.twig' %}

{% block title %}Logowanie{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>{{ controller_name }}</h1>
    <!-- FORM -->
    {{ message|raw }}    
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <!-- /FORM -->
</div>

{% endblock %}
